TODO:
--------


Backend:

	General:
	- Move DB-stuff into separate class
	- Add elements to context menu for tx_categories (create reference, paste into)	
	- Add elements to context menu for tables that are categorized (remove from category, copy reference) - only when in list module
	- Implement permission handling for backend users (Some backend users should only be allowed to see / modify certain parts of the tree). 
	- Implement an 'expand branch'  context menu function
	- Finish clipboard 

	BE-foms:
	- Finish free vocabulary tce field (90% done)
	- Make the User TSconfig option "TCAdefaults.[tablename].* = 1,2,3" work with category fields

	List-module:
	- Decide on which buttons to display in controlpanel (extended view)
	- Hook for adding extra buttons in controlpanel
	- Hook for adding content before and after a table
	- Hook for adding content to top and bottom


Frontend:

	General:
	- Implement FE API
	- Implement 'CMENU' content object for use in TYPO3 v 4.1 (using the new hook in tslib_cObj)


